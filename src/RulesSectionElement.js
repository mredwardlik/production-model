import SectionElement from './SectionElement'

/**
 * The class whic stores actions in a container.
 */
export default class RulesSectionElement extends SectionElement {

    constructor(rulesSectionElement) {
        super(rulesSectionElement)
    }

}